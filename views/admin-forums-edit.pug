head
    include include/head.pug
    title Edit #{forum.name}
body
    div(class="container")
        div(class="box center")
            h1(class="box-header text-center")
                | Edit #{forum.name}
            div(class="box-content")
                form(action="/admin/forums/"+forum.id+"/edit", method="post")
                    div(class="stacked-list")
                        span(class="form-input")
                            label(for="name") Name:
                            input(type="text", name="name", id="name" value=forum.name, autocomplete="off")
                        span(class="form-input")
                            label(for="description") Description:
                            input(type="textarea", name="description", id="description", maxlength="256", value=forum.description, autocomplete="off")
                        span(class="form-input")
                            label(for="identifier") Identifier:
                            input(type="text", name="identifier", id="identifier", value=forum.text_identifier, autocomplete="off")
                        button(type="submit") Save
            if error
                div(class="error mt-1 text-center") #{error}
        div(class="box center")
            h1(class="box-header text-center")
                | Delete #{forum.name}
            div(class="box-content stacked-list")
                p(class="text-center") Are you sure you want to delete this forum?
                a(href="/admin/forums/"+forum.id+"/delete") Yes, delete this forum.
                a(href="/admin/forums") No, go back.

                    

